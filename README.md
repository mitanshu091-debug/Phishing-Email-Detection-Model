# Phishing-Email-Detection-Model
Train on a dataset of phishing and legitimate emails, Extract and analyze email features (URLs, keywords, etc.), Classify emails as “Phishing” or “Safe”, Display accuracy and confusion matrix,
